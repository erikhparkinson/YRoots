PARAMETERS;
numThreads = 3;
PARAMETERS_END;

INTERVAL;
[-1, 1/pi];
[log(2), e];
INTERVAL_END;

FUNCTIONS;
function f1, f2;
variable_group x1, x2;
a = sqrt(x1^2 + x2^2)
f1 = sin(x1) + 2 + 2tan(a);
f2 = cos(a)/(2x2 + 1e-4) + 2 + log(x1 + 3);
FUNCTIONS_END;

END;
